#include "../shared/kernel.h"

void kernel_main(void) {
    hw_init();
    hw_print("Universal OS Core Loaded Successfully.\n");
    
    while(1) {
        // Main OS execution loop
    }
}
